return newFunction();
function newFunction() {
    var x = prompt("Guess a number between 1 and 10.");
    for (let i; >= 1; i < 11) {
        return i * (Math.random * Math.floor);
    }
    if (x === i) {
        alert("good work!");
    }
    else {
        alert("Not matched.");
    }
}
